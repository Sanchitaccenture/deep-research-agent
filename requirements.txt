# Core
python-dotenv>=1.0.1
pydantic>=2.8.0

# LangChain + LangGraph (LangGraph mode — required)
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.3.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3.0

# Search
tavily-python>=0.3.0

# RAG (upload/index documents)
chromadb>=0.5.0
sentence-transformers>=3.0.0
pypdf>=4.0.0

# API
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9
aiofiles>=23.0.0
sse-starlette>=2.1.0

# Legacy UI
streamlit>=1.36.0

# Tests + evals
pytest>=8.0.0
pytest-mock>=3.14.0
httpx>=0.27.0
pyyaml>=6.0.0

# NOTE: The Crew mode ships as a native LangChain+Groq implementation
# (see app/crew.py) — no `crewai` package required, works on Python 3.14.
# If you want the OFFICIAL crewai lib (adds delegation, memory, more tools),
# downgrade to Python 3.10-3.13 and `pip install -r requirements-crew.txt`.
